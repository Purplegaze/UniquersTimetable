package usecase.importsections;

public interface ImportTimetableInputBoundary {
    /**
     * Executes the export use case.
     * @param inputData the input data
     */
    void execute(ImportTimetableInputData inputData);

}
